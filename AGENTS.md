# Global Agent Rules

1. Keep files small and modular; split large files into reusable parts.
2. After each task, run `afplay /System/Library/Sounds/Glass.aiff`.
3. After each step, suggest a short commit title: `Implement dashboard` for features; `Fix: Problem` for fixes. Use no other prefixes.
4. After deploying Flutter to Chrome, press `r`; restart it if `pubspec.yaml` or assets changed.
